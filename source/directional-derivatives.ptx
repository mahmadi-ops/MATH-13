<?xml version="1.0" encoding="utf-8"?>
<chapter xml:id="section-directional-derivatives" xmlns:xi="http://www.w3.org/2001/XInclude">
<title>Directional Derivatives and Gradient Vectors</title>

<objectives>
<introduction>
<p>
After completing this section, you should be able to:
</p>
</introduction>

<ul>
<li>
<p>
Understand the directional derivative as the rate of change of
<m>f(x,y)</m> in an arbitrary direction, generalizing the partial
derivatives <m>f_x</m> and <m>f_y</m>, which are the special cases in the
<m>\mathbf i</m> and <m>\mathbf j</m> directions.
</p>
</li>
<li>
<p>
Interpret the directional derivative geometrically as the slope of the
tangent line to the curve in which a vertical plane through <m>P_0</m>,
in the direction of <m>\mathbf u</m>, cuts the surface
<m>z = f(x,y)</m>.
</p>
</li>
<li>
<p>
Understand the gradient vector <m>\nabla f</m> and its relationship to
the directional derivative through
<m>\left(D_{\mathbf u} f\right)_{P_0}
= \left(\nabla f\right)_{P_0}\cdot\mathbf u</m>.
</p>
</li>
<li>
<p>
Interpret the directional derivative through the angle between
<m>\nabla f</m> and <m>\mathbf u</m>, and understand why <m>f</m> changes
most rapidly in the direction of the gradient.
</p>
</li>
<li>
<p>
Understand why the gradient vector is perpendicular to the level curves
of <m>f</m>.
</p>
</li>
</ul>
</objectives>

<introduction>
<p>
Recall that the partial derivatives <m>f_x</m> and <m>f_y</m> correspond
to the rate of change of <m>z = f(x,y)</m> in the <m>\mathbf i</m> and
<m>\mathbf j</m> directions. In this section, we generalize this notion
to the rate of change of <m>z = f(x,y)</m> in an arbitrary direction
<m>\mathbf u</m>, which is a unit vector of the form
<m>\mathbf u = u_1\mathbf i + u_2\mathbf j</m>.
</p>

</introduction>

<!-- ================================================================ --><section xml:id="sec-dirderiv-definition">
<title>The Definition of the Directional Derivative</title>

<definition xml:id="def-directional-derivative">
<title>Directional Derivative</title>
<statement>
<p>
The <term>derivative of <m>f</m> at <m>P_0(x_0,y_0)</m> in the direction
of the unit vector <m>\mathbf u = u_1\mathbf i + u_2\mathbf j</m></term>
is the number
<md xml:id="eq-dirderiv-definition" number="yes">
\left(\frac{df}{ds}\right)_{\mathbf u,\,P_0}
= \lim_{s\to 0}\frac{f(x_0 + su_1,\; y_0 + su_2) - f(x_0,y_0)}{s},
</md>
provided the limit exists. It can alternatively be denoted by
<m>\left(D_{\mathbf u} f\right)_{P_0}</m>.
</p>
</statement>
</definition>

<p>
The geometry behind this definition is best seen in a picture. Starting at the
point <m>P_0(x_0,y_0)</m>, we move a distance <m>s</m> in the direction of the
unit vector <m>\mathbf u</m> to reach the point
<m>P(x_0 + su_1,\, y_0 + su_2)</m>, and we compare the values of the surface
<m>z = f(x,y)</m> above the two points. As <m>s \to 0</m>, the point <m>P</m>
slides back along <m>\mathbf u</m> toward <m>P_0</m>. The four figures that
follow show this from complementary angles:
<xref ref="fig-int-dirderiv-setup-3d"/> lets you explore the construction in
space, <xref ref="fig-dirderiv-setup-prefigure"/> records the displacement in
the <m>xy</m>-plane, <xref ref="fig-dirderiv-setup-video"/> animates the secant
line rotating onto the tangent line, and
<xref ref="fig-int-dirderiv-definition"/> reports the difference quotient itself
as <m>s</m> shrinks, so that the limit can be watched rather than only read.
</p>

<figure xml:id="fig-int-dirderiv-setup-3d">
<caption>
Interactive view of the definition of the directional derivative. The point <m>P</m> moves the distance <m>s</m> from <m>P_0</m> along <m>\mathbf u</m>, and the secant slope approaches <m>\left(D_{\mathbf u} f\right)_{P_0}</m> as <m>s \to 0</m>.
</caption>
<interactive xml:id="interactive-dirderiv-setup-3d" iframe="dirderiv-setup-3d.html" width="100%" aspect="8:6">
<instructions>
<p>
The translucent blue surface is the graph of <m>z = f(x,y)</m>. In the
<m>xy</m>-plane below it, the point <m>P_0(x_0,y_0)</m> is fixed, the
short blue arrow is the unit vector <m>\mathbf u</m>, and the gold arrow
is the displacement <m>s\mathbf u</m>, which ends at the point
<m>P(x_0+su_1,\, y_0+su_2)</m>. The dashed vertical lines rise from
<m>P_0</m> and <m>P</m> to the surface, where the values <m>f(P_0)</m>
and <m>f(P)</m> are marked. Drag the slider to change the distance
<m>s</m>, or press <q>s <m>\to</m> 0</q> to animate the limit in the
definition of the directional derivative: as <m>s \to 0</m>, the point
<m>P</m> slides back toward <m>P_0</m> and the difference quotient
<m>\big(f(P) - f(P_0)\big)/s</m>, displayed above the figure,
approaches <m>\left(D_{\mathbf u} f\right)_{P_0}</m>. Drag the figure
to view it from a different angle, or press <q>Rotate</q> to spin it.
</p>
</instructions>
</interactive>
</figure>

<figure xml:id="fig-dirderiv-setup-prefigure">
<caption>
The two points <m>P_0</m> and <m>P</m> used in the definition of the
directional derivative. As <m>s \to 0</m>, the point <m>P</m> slides
along the direction <m>\mathbf u</m> back toward <m>P_0</m>.
</caption>
<image width="55%">
<prefigure xmlns="https://prefigure.org" label="prefigure-dirderiv-setup">
<diagram dimensions="(340,280)" margins="10">
<coordinates bbox="(-0.4,-0.4,2.9,2.3)">
<axes at="ds-axes" xlabel="x" ylabel="y" decorations="no" arrows="1"/>
<line at="ds-comp1" endpoints="((0.65,0.55),(1.85,0.55))" stroke="#7F7F7F"
      dash="6 4" thickness="1.5"/>
<line at="ds-comp2" endpoints="((1.85,0.55),(1.85,1.45))" stroke="#7F7F7F"
      dash="6 4" thickness="1.5"/>
<vector at="ds-su" v="(1.2,0.9)" tail="(0.65,0.55)" stroke="#0072B2"
        thickness="3"/>
<vector at="ds-u" v="(0.48,0.36)" tail="(0.65,0.55)" stroke="#D55E00"
        thickness="3"/>
<point at="ds-P0" p="(0.65,0.55)" fill="#009E73" alignment="s">
<m>P_0(x_0,y_0)</m>
</point>
<point at="ds-P" p="(1.85,1.45)" fill="#009E73" alignment="n">
<m>P(x_0+su_1,\,y_0+su_2)</m>
</point>
<label at="ds-su-label" anchor="(1.15,1.15)" alignment="nw" color="#0072B2">
<m>s\mathbf u</m>
</label>
<label at="ds-comp1-label" anchor="(1.25,0.45)" alignment="s">
<m>su_1\mathbf i</m>
</label>
<label at="ds-comp2-label" anchor="(1.95,1.0)" alignment="e">
<m>su_2\mathbf j</m>
</label>
</coordinates>

<annotations>
<annotation ref="figure" text="The x y plane showing the setup for the definition of the directional derivative. From the point P 0 with coordinates x 0 comma y 0, a red arrow shows the unit vector u, and a longer blue arrow shows the displacement s u, ending at the point P with coordinates x 0 plus s u 1 comma y 0 plus s u 2. Dashed gray segments show the horizontal and vertical components of the displacement.">
  <annotation ref="ds-P0" text="The base point P 0 with coordinates x 0 comma y 0, where the directional derivative is computed."/>
  <annotation ref="ds-u" text="The unit vector u equals u 1 i plus u 2 j, drawn in red, giving the direction of differentiation."/>
  <annotation ref="ds-su" text="The displacement vector s u, drawn in blue, which moves the distance s from P 0 in the direction u."/>
  <annotation ref="ds-comp1" text="The horizontal component of the displacement, s u 1 i, drawn as a dashed segment."/>
  <annotation ref="ds-comp2" text="The vertical component of the displacement, s u 2 j, drawn as a dashed segment."/>
  <annotation ref="ds-P" text="The point P with coordinates x 0 plus s u 1 comma y 0 plus s u 2, which approaches P 0 as s goes to 0."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>

<figure xml:id="fig-dirderiv-setup-video">
<caption>
Animation of the definition of the directional derivative. The vertical
plane through <m>P_0</m> in the direction <m>\mathbf u</m> cuts the
surface <m>z = f(x,y)</m> in a curve, and the point
<m>P = P_0 + s\mathbf u</m> moves on this plane. The secant line through
<m>(P_0, f(P_0))</m> and <m>(P, f(P))</m> has slope
<m>\big(f(P) - f(P_0)\big)/s</m>, and as <m>s \to 0</m> it rotates onto
the tangent line, whose slope is the directional derivative
<m>\left(D_{\mathbf u} f\right)_{P_0}</m>.
</caption>
<video xml:id="video-dirderiv-setup"
       source="videos/dirderiv_secant_tangent_manim.mp4"
       width="100%" aspect="16:9"/>
</figure>

<figure xml:id="fig-int-dirderiv-definition">
<caption>
The difference quotient <m>\big(f(P)-f(P_0)\big)/s</m> for <m>f(x,y)=1+x^2+y^2</m> at <m>P_0(1,1)</m>, as functions of the direction angle and the step <m>s</m>.
</caption>
<interactive xml:id="interactive-dirderiv-definition" iframe="dirderiv-definition.html" width="100%" aspect="7:6">
<instructions>
<p>
Drag the slider for the angle to choose the unit vector <m>\mathbf u</m>,
and drag the slider for <m>s</m> to move the point <m>P</m> along the
direction <m>\mathbf u</m> starting at <m>P_0(1,1)</m>, for the function
<m>f(x,y) = 1 + x^2 + y^2</m>. The readout compares the difference
quotient <m>\big(f(P) - f(P_0)\big)/s</m> with its limit as
<m>s \to 0</m>, which is the directional derivative
<m>\left(D_{\mathbf u} f\right)_{P_0}</m>.
</p>
</instructions>
</interactive>
</figure>

<p>
Before developing any shortcuts, we evaluate a directional derivative straight
from <xref ref="def-directional-derivative"/>. Working through the limit once
makes the definition concrete, and it gives us a value to check against later:
<xref ref="example-dirderiv-gradient"/> recovers the same number in a couple of
lines, once the gradient is available.
</p>

<example xml:id="example-dirderiv-definition">
<title>Example I</title>
<statement>
<p>
Consider the function <m>f(x,y) = 1 + x^2 + y^2</m>, the point
<m>P_0(1,1)</m>, and the unit vector
<m>\mathbf u = \dfrac{1}{\sqrt 2}\mathbf i + \dfrac{1}{\sqrt 2}\mathbf j</m>.
Use the definition of directional derivative to compute
<m>\left(D_{\mathbf u} f\right)_{P_0}</m>.
</p>
</statement>


<solution>
<p>
We substitute <m>x_0 = y_0 = 1</m> and <m>u_1 = u_2 = \dfrac{1}{\sqrt 2}</m>
in the definition:
<md>
<mrow>
\left(\frac{df}{ds}\right)_{\mathbf u,P_0}
\amp= \lim_{s\to 0}\frac{f(x_0 + su_1,\, y_0 + su_2) - f(x_0,y_0)}{s}
</mrow>
<mrow>
\amp= \lim_{s\to 0}
\frac{f\!\left(1 + \tfrac{s}{\sqrt 2},\, 1 + \tfrac{s}{\sqrt 2}\right) - f(1,1)}{s}
</mrow>
<mrow>
\amp= \lim_{s\to 0}
\frac{1 + \left(1 + \tfrac{s}{\sqrt 2}\right)^2
+ \left(1 + \tfrac{s}{\sqrt 2}\right)^2 - 3}{s}
</mrow>
<mrow>\amp= \lim_{s\to 0}\frac{\tfrac{4s}{\sqrt 2} + s^2}{s} = \lim_{s\to 0}\left(\tfrac{4}{\sqrt 2} + s\right)</mrow>
<mrow>\amp= 2\sqrt 2.</mrow>
</md>
</p>
<p>
The computation has a useful geometric picture, illustrated in the
interactive 3D figure below. Along the ray through
<m>P_0(1,1)</m> in the direction <m>\mathbf u</m>, the values of <m>f</m>
are the single-variable function
<md>
<mrow>g(s) \amp= f\!\left(1 + \tfrac{s}{\sqrt 2},\, 1 + \tfrac{s}{\sqrt 2}\right)</mrow>
<mrow xml:id="eq-dirderiv-along-ray" number="yes">\amp= 3 + 2\sqrt 2\, s + s^2,</mrow>
</md>
and the difference quotient in the limit above is the slope of the
secant line of <m>g</m> through <m>s = 0</m> and <m>s</m>. As
<m>s \to 0</m> the secant lines approach the tangent line at
<m>s = 0</m>, whose slope is
<m>g'(0) = 2\sqrt 2 = \left(D_{\mathbf u} f\right)_{P_0}</m>.
</p>
<!-- Hidden: superseded by fig-int-dirderiv-secant-3d-manim below, which shows
     the same limit. Uncomment to bring it back.
<figure xml:id="fig-int-dirderiv-secant-3d">
<caption>
The secant line on the surface <m>z=1+x^2+y^2</m> rotating onto the tangent line as <m>s \to 0</m>, giving the directional derivative <m>2\sqrt2</m>.
</caption>
<interactive xml:id="interactive-dirderiv-secant-3d" iframe="dirderiv-secant-3d.html" width="100%" aspect="8:6">
<instructions>
<p>
The surface <m>z = 1 + x^2 + y^2</m> is cut by the vertical plane through
<m>P_0(1,1)</m> in the direction <m>\mathbf u</m>. Drag the slider to
change <m>s</m>: the point <m>P</m> slides along <m>\mathbf u</m> in the
<m>xy</m>-plane, and the secant line through the surface points
<m>(1,\,1,\,f(P_0))</m> and <m>(1+su_1,\,1+su_2,\,f(P))</m> rotates
toward the tangent line. Press <q>s <m>\to</m> 0</q> to animate the
limit; the readout shows the secant slope <m>2\sqrt 2 + s</m>
approaching the directional derivative <m>2\sqrt 2</m>. Drag the figure
to view it from a different angle, or press the <q>Rotate</q> button.
</p>
</instructions>
</interactive>
</figure>
-->

<figure xml:id="fig-int-dirderiv-secant-3d-manim">
<caption>
The secant through <m>(P_0, f(P_0))</m> and <m>(P, f(P))</m> on the
surface <m>z = 1 + x^2 + y^2</m>, rotating onto the tangent line at
<m>P_0</m> as <m>s \to 0</m>, with slope
<m>2\sqrt2 + s \to 2\sqrt2</m>.
</caption>
<interactive xml:id="interactive-dirderiv-secant-3d-manim" iframe="dirderiv-secant-3d-manim.html" width="100%" aspect="1:1">
<instructions>
<p>
Press <q>Play</q> to watch the whole construction in one continuous
motion: the vertical plane through <m>P_0(1,1)</m> in the direction
<m>\mathbf u</m> cuts the surface <m>z = 1 + x^2 + y^2</m> in the gold
curve, the red secant joins <m>(P_0, f(P_0))</m> to <m>(P, f(P))</m>,
and as <m>s \to 0</m> that secant rotates onto the green tangent line.
The reading at the lower left compares the secant slope
<m>2\sqrt 2 + s</m> with the tangent slope <m>2\sqrt 2</m>, which is the
directional derivative <m>\left(D_{\mathbf u} f\right)_{P_0}</m>. Stop
the animation at any time and drag the <m>s</m> slider yourself, or
press <q>s <m>\to</m> 0</q> to replay just the limit. Drag the figure to
change the viewpoint, or press <q>Spin</q> to let it turn.
</p>
</instructions>
</interactive>
</figure>
</solution>
</example>


<activity xml:id="activity-dirderiv-partials">
<title>The Partial Derivatives Are Directional Derivatives</title>

<introduction>
<p>
<xref ref="def-directional-derivative"/> measures the rate of change of <m>f</m> in
an arbitrary direction <m>\mathbf u</m>. Two directions are already familiar from
<m>f_x</m> and <m>f_y</m>. In this activity you will check that the definition
reproduces exactly those two derivatives when <m>\mathbf u</m> points along a
coordinate axis. Use only <xref ref="eq-dirderiv-definition"/>; nothing beyond the
definition is needed.
</p>
</introduction>

<task xml:id="task-dirderiv-i">
<statement>
<p>
Take <m>\mathbf u = \mathbf i</m>, so that <m>u_1 = 1</m> and <m>u_2 = 0</m>.
Substitute these into <xref ref="eq-dirderiv-definition"/> and simplify. Which
single-variable limit is left, and what is its value?
</p>
</statement>
<hint>
<p>
Ask yourself which of the two inputs of <m>f</m> actually changes as <m>s</m> varies.
</p>
</hint>
<answer>
<p>
Substituting <m>u_1 = 1</m> and <m>u_2 = 0</m> leaves the second slot of <m>f</m>
fixed at <m>y_0</m>:
<md>
<mrow>
\left(\frac{df}{ds}\right)_{\mathbf i,\,P_0}
\amp= \lim_{s\to 0}\frac{f(x_0 + s\cdot 1,\; y_0 + s\cdot 0) - f(x_0,y_0)}{s}
</mrow>
<mrow>
\amp= \lim_{s\to 0}\frac{f(x_0 + s,\; y_0) - f(x_0,y_0)}{s}
= f_x(x_0,y_0).
</mrow>
</md>
</p>
<p>
Only the first variable moves, so this is precisely the limit that defines the partial
derivative with respect to <m>x</m>. That is,
<m>\left(D_{\mathbf i} f\right)_{P_0} = f_x(x_0,y_0)</m>.
</p>
</answer>
</task>

<task xml:id="task-dirderiv-j">
<statement>
<p>
Repeat the computation with <m>\mathbf u = \mathbf j</m>, and state the conclusion.
</p>
</statement>
<answer>
<p>
Now <m>u_1 = 0</m> and <m>u_2 = 1</m>, so the first slot is held at <m>x_0</m>:
<md>
<mrow>
\left(\frac{df}{ds}\right)_{\mathbf j,\,P_0}
\amp= \lim_{s\to 0}\frac{f(x_0,\; y_0 + s) - f(x_0,y_0)}{s}
= f_y(x_0,y_0),
</mrow>
</md>
so <m>\left(D_{\mathbf j} f\right)_{P_0} = f_y(x_0,y_0)</m>. The partial derivatives
are the directional derivatives in the two coordinate directions, and
<m>D_{\mathbf u} f</m> is the common generalization of both.
</p>
</answer>
</task>

<task xml:id="task-dirderiv-check">
<statement>
<p>
Test this on the function of <xref ref="example-dirderiv-definition"/>. With
<m>f(x,y) = 1 + x^2 + y^2</m> and <m>P_0(1,1)</m>, compute
<m>\left(D_{\mathbf i} f\right)_{P_0}</m> straight from the definition, then compute
<m>f_x(1,1)</m> in the usual way and compare.
</p>
</statement>
<answer>
<p>
From the definition,
<md>
<mrow>
\left(D_{\mathbf i} f\right)_{P_0}
\amp= \lim_{s\to 0}\frac{f(1+s,\,1) - f(1,1)}{s}
= \lim_{s\to 0}\frac{\left[1 + (1+s)^2 + 1\right] - 3}{s}
</mrow>
<mrow>\amp= \lim_{s\to 0}\frac{2s + s^2}{s} = \lim_{s\to 0}\,(2 + s)</mrow>
<mrow>\amp= 2 .</mrow>
</md>
</p>
<p>
Differentiating instead, <m>f_x = 2x</m>, so <m>f_x(1,1) = 2</m>. The two agree. By
symmetry <m>\left(D_{\mathbf j} f\right)_{P_0} = f_y(1,1) = 2</m> as well. Note that
both are smaller than the value <m>2\sqrt 2</m> found in
<xref ref="example-dirderiv-definition"/>: the function climbs faster along the
diagonal than along either axis.
</p>
</answer>
</task>

<task xml:id="task-dirderiv-negative">
<statement>
<p>
Finally, take <m>\mathbf u = -\mathbf i</m>. Show that
<m>\left(D_{-\mathbf i} f\right)_{P_0} = -f_x(x_0,y_0)</m>, and say in a sentence why
this is what you should expect.
</p>
</statement>
<answer>
<p>
Here <m>u_1 = -1</m> and <m>u_2 = 0</m>, so
<md>
<mrow>
\left(D_{-\mathbf i} f\right)_{P_0}
\amp= \lim_{s\to 0}\frac{f(x_0 - s,\; y_0) - f(x_0,y_0)}{s} .
</mrow>
</md>
</p>
<p>
Putting <m>h = -s</m> turns this into
<m>-\lim_{h\to 0}\dfrac{f(x_0+h,\,y_0) - f(x_0,y_0)}{h} = -f_x(x_0,y_0)</m>.
Walking the opposite way along the same line makes <m>f</m> change at the same rate
with the opposite sign, so reversing <m>\mathbf u</m> reverses the sign of
<m>D_{\mathbf u} f</m>.
</p>
</answer>
</task>

<conclusion>
<p>
Every partial derivative is a directional derivative, so nothing already known about
<m>f_x</m> and <m>f_y</m> is lost. What the definition adds is the freedom to point
<m>\mathbf u</m> anywhere. Keep in mind that this depends on <m>\mathbf u</m> being a
<em>unit</em> vector: the parameter <m>s</m> measures actual distance travelled in the
<m>xy</m>-plane only when <m>\|\mathbf u\| = 1</m>.
</p>
</conclusion>

</activity>

</section>

<!-- ================================================================ -->
<section xml:id="sec-gradient-vector">
<title>The Gradient Vector</title>

<p>
Note that we can write the equation of the line through the point
<m>P_0(x_0,y_0)</m> with the direction
<m>\mathbf u = u_1\mathbf i + u_2\mathbf j</m> as
<md xml:id="eq-line-through-P0" number="yes">
x(s) = x_0 + su_1, \qquad y(s) = y_0 + su_2.
</md>
</p>

<p>
We can use the above relation between <m>x</m>, <m>y</m>, and <m>s</m>
and the chain rule, whose dependency diagram is shown in
<xref ref="fig-chain-rule-tree-prefigure"/>, to compute the directional
derivative as follows:
<md>
<mrow>\left(D_{\mathbf u} f\right)_{P_0} = \left(\frac{df}{ds}\right)_{\mathbf u,P_0} \amp= \left(\frac{\partial f}{\partial x}\right)_{P_0}\frac{dx}{ds}</mrow>
<mrow>\amp\quad + \left(\frac{\partial f}{\partial y}\right)_{P_0}\frac{dy}{ds}</mrow>
<mrow>\amp= \left(\frac{\partial f}{\partial x}\right)_{P_0}\cdot u_1</mrow>
<mrow>\amp\quad + \left(\frac{\partial f}{\partial y}\right)_{P_0}\cdot u_2</mrow>
<mrow>
\amp= \left[\left(\frac{\partial f}{\partial x}\right)_{P_0}\mathbf i
+ \left(\frac{\partial f}{\partial y}\right)_{P_0}\mathbf j\right]
\cdot \left(u_1\mathbf i + u_2\mathbf j\right).
</mrow>
</md>
</p>
<p>
The vector in the first set of parentheses is known as the
<term>gradient vector</term>.
</p>

<definition xml:id="def-gradient-vector">
<title>Gradient Vector</title>
<statement>
<p>
The <term>gradient vector</term> of the function <m>f(x,y)</m> at
<m>P_0(x_0,y_0)</m> is the vector
<md xml:id="eq-gradient-definition" number="yes">
\left(\nabla f\right)_{P_0}
= \left(\frac{\partial f}{\partial x}\right)_{P_0}\mathbf i
+ \left(\frac{\partial f}{\partial y}\right)_{P_0}\mathbf j.
</md>
</p>

<p>
The gradient vector and the directional derivative of <m>f(x,y)</m> at
<m>P_0(x_0,y_0)</m> in the direction
<m>\mathbf u = u_1\mathbf i + u_2\mathbf j</m> are related via the
following dot product:
<md xml:id="eq-dirderiv-gradient-relation" number="yes">
\left(\frac{df}{ds}\right)_{\mathbf u,\,P_0}
= \left(\nabla f\right)_{P_0}\cdot\mathbf u.
</md>
</p>
</statement>
</definition>

<figure xml:id="fig-chain-rule-tree-prefigure">
<caption>
The chain rule dependency diagram: multiplying down each branch and
adding the two products gives
<m>\frac{df}{ds} = \frac{\partial f}{\partial x}\frac{dx}{ds}
+ \frac{\partial f}{\partial y}\frac{dy}{ds}</m>.
</caption>
<image width="48%">
<prefigure xmlns="https://prefigure.org" label="prefigure-chain-rule-tree">
<diagram dimensions="(300,280)" margins="10">
<coordinates bbox="(-3.0,-0.7,3.0,3.7)">
<label at="ct-z" anchor="(0,3.1)" alignment="c" scale="1.4">
<m>z=f(x,y)</m>
</label>
<label at="ct-x" anchor="(-1.7,1.5)" alignment="c" scale="1.4">
<m>x</m>
</label>
<label at="ct-y" anchor="(1.7,1.5)" alignment="c" scale="1.4">
<m>y</m>
</label>
<label at="ct-s" anchor="(0,-0.1)" alignment="c" scale="1.4">
<m>s</m>
</label>
<vector at="ct-zx" v="(-1.1,-1.05)" tail="(-0.45,2.85)" stroke="#0072B2"
        thickness="2"/>
<vector at="ct-zy" v="(1.1,-1.05)" tail="(0.45,2.85)" stroke="#0072B2"
        thickness="2"/>
<vector at="ct-xs" v="(1.25,-1.05)" tail="(-1.5,1.25)" stroke="#D55E00"
        thickness="2"/>
<vector at="ct-ys" v="(-1.25,-1.05)" tail="(1.5,1.25)" stroke="#D55E00"
        thickness="2"/>
<label at="ct-dfdx" anchor="(-1.75,2.55)" alignment="w" color="#0072B2" scale="1.4">
<m>\dfrac{\partial f}{\partial x}</m>
</label>
<label at="ct-dfdy" anchor="(1.75,2.55)" alignment="e" color="#0072B2" scale="1.4">
<m>\dfrac{\partial f}{\partial y}</m>
</label>
<label at="ct-dxds" anchor="(-1.6,0.55)" alignment="w" color="#D55E00" scale="1.4">
<m>\dfrac{dx}{ds}</m>
</label>
<label at="ct-dyds" anchor="(1.6,0.55)" alignment="e" color="#D55E00" scale="1.4">
<m>\dfrac{dy}{ds}</m>
</label>
</coordinates>

<annotations>
<annotation ref="figure" text="A dependency tree for the chain rule. At the top is z equals f of x comma y. Blue arrows labeled partial f partial x and partial f partial y lead down to the intermediate variables x and y. Red arrows labeled d x d s and d y d s lead from x and y down to the single independent variable s.">
  <annotation ref="ct-z" text="The dependent variable z equals f of x comma y at the top of the tree."/>
  <annotation ref="ct-zx" text="The branch from z to x, labeled with the partial derivative of f with respect to x."/>
  <annotation ref="ct-zy" text="The branch from z to y, labeled with the partial derivative of f with respect to y."/>
  <annotation ref="ct-xs" text="The branch from x down to s, labeled d x d s, which equals u 1 along the line through P 0."/>
  <annotation ref="ct-ys" text="The branch from y down to s, labeled d y d s, which equals u 2 along the line through P 0."/>
  <annotation ref="ct-s" text="The independent variable s, the arc length along the line through P 0 in the direction u."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>

<p>
A fundamental geometric property of the gradient is that, at every point,
it is perpendicular to the level curve of <m>f</m> passing through that
point, as the interactive figure below lets you verify. We state and
prove this fact now.
</p>

<theorem xml:id="thm-gradient-perp-level-curve">
<title>The Gradient is Perpendicular to the Level Curves</title>
<statement>
<p>
Let <m>f(x,y)</m> be a differentiable function, and let <m>C</m> be the
level curve <m>f(x,y) = c</m> for a constant <m>c</m>. At every point of
<m>C</m> where <m>\nabla f \neq \mathbf 0</m>, the gradient vector
<m>\nabla f</m> is perpendicular to the level curve <m>C</m>.
</p>
</statement>
<proof>
<p>
Suppose the level curve <m>C</m> is parametrized by a differentiable
vector function
<md xml:id="eq-level-curve-param" number="yes">
\mathbf r(t) = \langle x(t),\, y(t)\rangle,
</md>
and let <m>\mathbf v(t) = \langle x'(t),\, y'(t)\rangle</m> denote its
velocity vector. Because every point of <m>C</m> lies on the level curve,
its coordinates satisfy
<md xml:id="eq-level-curve-constant" number="yes">
f\bigl(x(t),\, y(t)\bigr) = c \qquad \text{for all } t.
</md>
</p>

<p>
The left-hand side is constant in <m>t</m>, so its derivative is zero.
Differentiating both sides with respect to <m>t</m> and applying the
chain rule gives
<md>
<mrow>\frac{d}{dt}\, f\bigl(x(t), y(t)\bigr) \amp= \frac{\partial f}{\partial x}\,\frac{dx}{dt} + \frac{\partial f}{\partial y}\,\frac{dy}{dt}</mrow>
<mrow xml:id="eq-level-curve-chain-rule" number="yes">\amp= 0.</mrow>
</md>
</p>
<p>
We recognize the middle expression as the dot product of the gradient
vector with the velocity vector:
<md>
<mrow>\left\langle \frac{\partial f}{\partial x},\, \frac{\partial f}{\partial y} \right\rangle \cdot \left\langle \frac{dx}{dt},\, \frac{dy}{dt} \right\rangle \amp= \nabla f \cdot \mathbf v(t)</mrow>
<mrow xml:id="eq-level-curve-dot-product" number="yes">\amp= 0.</mrow>
</md>
</p>
<p>
Therefore, at each point of <m>C</m>, the gradient <m>\nabla f</m> is
orthogonal to the velocity vector <m>\mathbf v(t)</m>, as shown in
<xref ref="fig-gradient-perp-prefigure"/>. Since the velocity vector
points in the direction of motion along the curve, it is tangent to
<m>C</m>; hence <m>\nabla f</m> being orthogonal to <m>\mathbf v(t)</m>
means precisely that <m>\nabla f</m> is perpendicular to the level curve
<m>C</m>. This holds at every point where <m>\nabla f \neq \mathbf 0</m>.
</p>
</proof>
</theorem>

<figure xml:id="fig-gradient-perp-prefigure">
<caption>
The gradient <m>\nabla f</m> meets the level curve <m>C\colon f = c</m>
at a right angle: it is perpendicular to the velocity vector
<m>\mathbf v(t)</m>, which is tangent to <m>C</m>.
</caption>
<image width="58%">
<prefigure xmlns="https://prefigure.org" label="prefigure-gradient-perp">
<diagram dimensions="(360,320)" margins="10">
<definition>ce(t) = (2.4*cos(t), 1.6*sin(t))</definition>
<definition>ci(t) = (1.63*cos(t), 1.09*sin(t))</definition>
<definition>co(t) = (3.12*cos(t), 2.08*sin(t))</definition>
<coordinates bbox="(-3.7,-2.7,3.7,2.9)">
<parametric-curve at="gp-inner" function="ci" domain="(0,6.2832)"
                  stroke="#7F7F7F"/>
<parametric-curve at="gp-outer" function="co" domain="(0,6.2832)"
                  stroke="#7F7F7F"/>
<parametric-curve at="gp-C" function="ce" domain="(0,6.2832)"
                  stroke="#009E73" thickness="2.5"/>
<line at="gp-ra1" endpoints="((1.470,1.510),(1.271,1.603))"
      stroke="black" thickness="1.5"/>
<line at="gp-ra2" endpoints="((1.271,1.603),(1.178,1.404))"
      stroke="black" thickness="1.5"/>
<vector at="gp-grad" v="(0.465,0.997)" tail="(1.377,1.311)"
        stroke="#D55E00" thickness="3"/>
<vector at="gp-vel" v="(-0.997,0.465)" tail="(1.377,1.311)"
        stroke="#0072B2" thickness="3"/>
<point at="gp-P" p="(1.377,1.311)" fill="black" alignment="se">
<m>P</m>
</point>
<label at="gp-grad-label" anchor="(1.95,2.4)" alignment="ne" color="#D55E00">
<m>\nabla f</m>
</label>
<label at="gp-vel-label" anchor="(0.3,1.85)" alignment="nw" color="#0072B2">
<m>\mathbf v(t)</m>
</label>
<label at="gp-C-label" anchor="(0,-1.6)" alignment="c" clear-background="yes" color="#009E73">
<m>C\colon f=c</m>
</label>
<label at="gp-outer-label" anchor="(0,-2.08)" alignment="c" clear-background="yes">
<m>f=c+\Delta c</m>
</label>
<label at="gp-inner-label" anchor="(0,-1.09)" alignment="c" clear-background="yes">
<m>f=c-\Delta c</m>
</label>
</coordinates>

<annotations>
<annotation ref="figure" text="Three nested oval level curves of f. The middle green oval is the level curve C where f equals c. At a point P on C, a blue arrow is the velocity vector v of t, tangent to C, and a red arrow is the gradient of f, perpendicular to the velocity and pointing outward toward the larger level curve. A small square at P marks the right angle between them.">
  <annotation ref="gp-C" text="The level curve C, where f equals c, drawn in green."/>
  <annotation ref="gp-P" text="A point P on the level curve C."/>
  <annotation ref="gp-vel" text="The velocity vector v of t, drawn in blue, tangent to the level curve and pointing in the direction of motion along it."/>
  <annotation ref="gp-grad" text="The gradient vector of f, drawn in red, perpendicular to the velocity vector and pointing outward toward the level curve of larger value."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>

<activity xml:id="activity-gradient-perp-check">
<title>Checking that the Gradient Meets Level Curves at Right Angles</title>

<introduction>
<p>
<xref ref="thm-gradient-perp-level-curve"/> claims that <m>\nabla f</m> is
perpendicular to the level curve through a point. This activity asks you to verify
that claim by hand at one point, and then to test it at many points using the
interactive figure that follows.
</p>
</introduction>

<task xml:id="task-perp-level-curve">
<statement>
<p>
Let <m>f(x,y) = 1 + x^2 + y^2</m>. Find the level curve of <m>f</m> through
<m>P_0(1,1)</m>, and give a vector tangent to that curve at <m>P_0</m>.
</p>
</statement>
<hint>
<p>
Level curves of this <m>f</m> are circles centred at the origin. A tangent to a
circle is perpendicular to its radius.
</p>
</hint>
<answer>
<p>
Since <m>f(1,1) = 3</m>, the level curve is <m>1 + x^2 + y^2 = 3</m>, that is the
circle <m>x^2 + y^2 = 2</m> of radius <m>\sqrt 2</m>. The radius at <m>P_0</m>
points along <m>\mathbf i + \mathbf j</m>, so a tangent vector is
<m>\mathbf T = -\mathbf i + \mathbf j</m>.
</p>
</answer>
</task>

<task xml:id="task-perp-dot-product">
<statement>
<p>
Compute <m>\left(\nabla f\right)_{(1,1)}</m> and then the dot product of that
gradient with your tangent vector. What does the result say?
</p>
</statement>
<answer>
<p>
<m>\nabla f = 2x\,\mathbf i + 2y\,\mathbf j</m> gives
<m>\left(\nabla f\right)_{(1,1)} = 2\,\mathbf i + 2\,\mathbf j</m>, and
<md>
<mrow>
\left(\nabla f\right)_{(1,1)} \cdot \mathbf T
\amp= (2)(-1) + (2)(1) = 0 .
</mrow>
</md>
</p>
<p>
A zero dot product between non-zero vectors means the two are orthogonal, so
<m>\nabla f</m> meets the level curve at a right angle at <m>P_0</m>, exactly as
<xref ref="thm-gradient-perp-level-curve"/> asserts.
</p>
</answer>
</task>

<task xml:id="task-perp-general-point">
<statement>
<p>
Repeat the argument at an arbitrary point <m>(a,b) \ne (0,0)</m> to show the right
angle is not a coincidence of the point <m>(1,1)</m>.
</p>
</statement>
<answer>
<p>
The level curve through <m>(a,b)</m> is the circle <m>x^2 + y^2 = a^2 + b^2</m>,
whose tangent there is <m>\mathbf T = -b\,\mathbf i + a\,\mathbf j</m>. Since
<m>\left(\nabla f\right)_{(a,b)} = 2a\,\mathbf i + 2b\,\mathbf j</m>,
<md>
<mrow>
\left(\nabla f\right)_{(a,b)} \cdot \mathbf T
\amp= 2a(-b) + 2b(a) = 0 ,
</mrow>
</md>
so the gradient is perpendicular to the level curve at every point other than the
origin, where <m>\nabla f = \mathbf 0</m> and no direction is singled out.
</p>
</answer>
</task>

<task xml:id="task-perp-interactive">
<statement>
<p>
Now drag the red point in <xref ref="fig-int-gradient-level-curves"/> to several locations,
including points on the axes and points far from the origin. Confirm that the
gradient arrow always meets the level curve at a right angle, and describe how the
length of the arrow changes as the point moves away from the origin. Can you place
the point where the gradient vanishes?
</p>

<figure xml:id="fig-int-gradient-level-curves">
<caption>
The gradient <m>\nabla f</m> of <m>f(x,y)=1+x^2+y^2</m> at a movable point, shown with the level curve through that point, illustrating that <m>\nabla f</m> is perpendicular to the level curves.
</caption>
<interactive xml:id="interactive-gradient-level-curves" iframe="gradient-level-curves.html" width="100%" aspect="7:6">
<instructions>
<p>
Drag the red point anywhere in the plane. The gradient vector
<m>\nabla f = 2x\,\mathbf i + 2y\,\mathbf j</m> of
<m>f(x,y) = 1 + x^2 + y^2</m> is drawn at the point, together with the
level curve of <m>f</m> through the point. Observe that the gradient is
always perpendicular to the level curve and points in the direction in
which <m>f</m> increases.
</p>
</instructions>
</interactive>
</figure>
</statement>
<answer>
<p>
The right angle persists everywhere. The arrow lengthens as the point moves
outward, since <m>\|\nabla f\| = 2\sqrt{x^2 + y^2}</m> grows with the distance from
the origin; correspondingly the level curves are spaced more closely there, because
<m>f</m> is climbing faster. At the origin <m>\nabla f = \mathbf 0</m>: the level
curve degenerates to a single point and there is no direction of steepest ascent.
</p>
</answer>
</task>

</activity>

<p>
With <xref ref="eq-dirderiv-gradient-relation"/> in hand, the limit that defined
the directional derivative can be set aside: the gradient and a dot product do
the same work in a couple of lines. To see the difference, we rework
<xref ref="example-dirderiv-definition"/> <mdash/> the same function, point, and
direction <mdash/> and then read the answer geometrically off the surface.
</p>

<example xml:id="example-dirderiv-gradient">
<title>Example II</title>
<statement>
<p>
Consider the function <m>f(x,y) = 1 + x^2 + y^2</m>, the point
<m>P_0(1,1)</m>, and the unit vector
<m>\mathbf u = \dfrac{1}{\sqrt 2}\mathbf i + \dfrac{1}{\sqrt 2}\mathbf j</m>.
Evaluate the directional derivative
<m>\left(D_{\mathbf u} f\right)_{P_0}</m> and interpret your result.
</p>
</statement>


<solution>
<p>
We use
<m>\left(\dfrac{df}{ds}\right)_{\mathbf u,P_0}
= \left(\nabla f\right)_{P_0}\cdot\mathbf u</m>.
First we compute the gradient vector:
<md>
<mrow>\nabla f \amp= \frac{\partial f}{\partial x}\,\mathbf i
        + \frac{\partial f}{\partial y}\,\mathbf j</mrow>
<mrow>\amp= (2x)\,\mathbf i + (2y)\,\mathbf j,</mrow>
<mrow>\left(\nabla f\right)_{(1,1)}
        \amp= \left(\frac{\partial f}{\partial x}\right)_{(1,1)}\mathbf i
        + \left(\frac{\partial f}{\partial y}\right)_{(1,1)}\mathbf j</mrow>
<mrow>\amp= 2\,\mathbf i + 2\,\mathbf j.</mrow>
</md>
</p>
<p>
Then we take the dot product with <m>\mathbf u</m>:
<md>
<mrow>\left(\frac{df}{ds}\right)_{\mathbf u,P_0} \amp= \left(\nabla f\right)_{P_0}\cdot\mathbf u</mrow>
<mrow>\amp= \left(2\,\mathbf i + 2\,\mathbf j\right)\cdot
        \left(\tfrac{1}{\sqrt 2}\,\mathbf i + \tfrac{1}{\sqrt 2}\,\mathbf j\right)</mrow>
<mrow>\amp= \frac{4}{\sqrt 2}</mrow>
<mrow xml:id="eq-gradient-example-dot" number="yes">\amp= 2\sqrt 2,</mrow>
</md>
which agrees with the limit computation in
<xref ref="example-dirderiv-definition"/>. See
<xref ref="fig-dirderiv-surface-tikz"/> for a geometrical interpretation
of the directional derivative. Note that the unit vector
<m>\mathbf u = \frac{1}{\sqrt 2}\mathbf i + \frac{1}{\sqrt 2}\mathbf j</m>
is the direction vector of the line <m>y = x</m> located in the
<m>xy</m>-plane. The plane <m>x - y = 0</m> contains this line and is
orthogonal to the <m>xy</m>-plane. The intersection of the surface
<m>z = 1 + x^2 + y^2</m> and the plane <m>x - y = 0</m> is the parabola
shown in the figure. Finally, the directional derivative of
<m>f(x,y) = 1 + x^2 + y^2</m> at the point <m>(1,1)</m> corresponds to
the slope of the tangent line to the surface <m>z = 1 + x^2 + y^2</m> at
<m>(1,1,3)</m> that is located in the plane <m>x - y = 0</m>.
</p>
<figure xml:id="fig-dirderiv-surface-tikz">
<caption>
The surface <m>z = 1 + x^2 + y^2</m> cut by the vertical plane
<m>x - y = 0</m>, which contains the direction <m>\mathbf u</m>. The
intersection is a parabola, and the directional derivative
<m>\left(D_{\mathbf u} f\right)_{(1,1)} = 2\sqrt 2</m> is the slope of
its tangent line at the point <m>(1,1,3)</m>. The level curve
<m>f = 3</m> through <m>(1,1)</m>, the circle <m>x^2 + y^2 = 2</m>, is
drawn on the surface at height <m>z = 3</m>, with its dashed projection
in the <m>xy</m>-plane.
</caption>
<image xml:id="image-dirderiv-surface-tikz" width="70%">
<shortdescription>
A paraboloid cut by a vertical plane, with the trace parabola and its
tangent line at the point one comma one comma three.
</shortdescription>
<description>
<p>
The surface <m>z = 1 + x^2 + y^2</m>, an upward-opening paraboloid,
drawn as a translucent blue surface. A translucent yellow vertical
plane <m>x - y = 0</m> passes through the surface above the line
<m>y = x</m> in the <m>xy</m>-plane, where the unit vector
<m>\mathbf u</m> is drawn as a blue arrow starting at <m>(1,1,0)</m>.
The plane meets the surface in a parabola, drawn in solid vermillion,
and a green tangent line touches this parabola at the marked point
<m>(1,1,3)</m>. The slope of this tangent line, measured within the
vertical plane, is the directional derivative <m>2\sqrt 2</m>. A
reddish-purple circle on the surface at height <m>z = 3</m> marks the
level curve <m>f = 3</m>, and its dashed projection in the
<m>xy</m>-plane is the circle <m>x^2 + y^2 = 2</m> through the point
<m>(1,1)</m>.
</p>
</description>
<latex-image>
<![CDATA[\begin{tikzpicture}[line join=round, line cap=round]
  % Okabe-Ito colorblind-safe palette
  \definecolor{oiblue}{RGB}{0,114,178}
  \definecolor{oivermillion}{RGB}{213,94,0}
  \definecolor{oigreen}{RGB}{0,158,115}
  \definecolor{oiyellow}{RGB}{240,228,66}
  \definecolor{oipurple}{RGB}{204,121,167}
  \begin{axis}[
      view={115}{18},
      width=11.5cm,
      axis lines=center,
      axis line style={-latex, black!75, line width=0.7pt},
      xlabel={$x$}, ylabel={$y$}, zlabel={$z$},
      label style={font=\large},
      ticks=none,
      xmin=-2.1, xmax=2.1, ymin=-2.1, ymax=2.1, zmin=0, zmax=4.9,
      clip=false,
      legend style={
        at={(0.5,1.04)}, anchor=south,
        legend columns=2,
        font=\scriptsize,
        draw=black!35, rounded corners=2pt,
        fill=white, fill opacity=0.92, text opacity=1,
        inner sep=4pt, row sep=2pt, column sep=10pt,
      },
      legend cell align=left,
      legend image post style={line width=1.4pt, sharp plot},
    ]
    % ---- vertical plane x - y = 0 (drawn first, behind surface) ----
    \fill[oiyellow, opacity=0.28]
      (axis cs:-1.7,-1.7,0) -- (axis cs:1.7,1.7,0)
      -- (axis cs:1.7,1.7,4.4) -- (axis cs:-1.7,-1.7,4.4) -- cycle;
    % ---- surface: paraboloid z = 1 + x^2 + y^2 ----
    \addplot3[surf, shader=interp,
              point meta={z},
              colormap={softz}{color=(oiblue!12!white) color=(oiblue!38!white)},
              opacity=0.5, fill opacity=0.5,
              domain=0:360, samples=61,
              y domain=0:1.7, samples y=25,
              z buffer=sort, forget plot]
      ({y*cos(x)}, {y*sin(x)}, {1 + y^2});
    % surface listed in the legend (so the equation never overlaps the plot)
    \addlegendimage{area legend, fill=oiblue!30, draw=oiblue!45}
    \addlegendentry{surface $z=1+x^2+y^2$}
    % light parallels (circles) for shape cues
    \foreach \rr in {0.5,0.9,1.3,1.7}{
      \edef\tmp{\noexpand\addplot3[black!22, line width=0.35pt, domain=0:360,
          samples=97, samples y=0, forget plot]
        ({\rr*cos(x)}, {\rr*sin(x)}, {1 + \rr*\rr});}\tmp}
    % ---- trace: parabola z = 1 + 2t^2 along x = y = t ----
    \addplot3[line width=1.7pt, oivermillion, domain=-1.2:1.2,
        samples=61, samples y=0]
      (x, x, {1 + 2*x^2});
    \addlegendentry{trace in the plane $x-y=0$}
    % ---- tangent line at (1,1,3) ----
    \addplot3[line width=1.7pt, oigreen, domain=-0.55:0.55,
        samples=2, samples y=0]
      ({1 + x/sqrt(2)}, {1 + x/sqrt(2)}, {3 + 2*sqrt(2)*x});
    \addlegendentry{tangent line, slope $2\sqrt2$}
    % ---- level curve f = 3 through (1,1): x^2 + y^2 = 2 ----
    \addplot3[line width=1.6pt, oipurple, domain=0:360, samples=121,
        samples y=0]
      ({sqrt(2)*cos(x)}, {sqrt(2)*sin(x)}, 3);
    \addlegendentry{level curve $x^2+y^2=2$}
    \addplot3[line width=1.2pt, oipurple, dashed, domain=0:360, samples=121,
        samples y=0, forget plot]
      ({sqrt(2)*cos(x)}, {sqrt(2)*sin(x)}, 0);
    % ---- dashed drop line and marked point ----
    \addplot3[black!60, dashed, line width=0.6pt, samples y=0, forget plot]
      coordinates {(1,1,0) (1,1,3)};
    \addplot3[only marks, mark=*, mark size=1.8pt, color=black, forget plot]
      coordinates {(1,1,3)};
    \node[anchor=west, font=\small] at (axis cs:1.1,1.1,3.3) {$(1,1,3)$};
    % ---- unit vector u in the xy-plane ----
    \draw[-{Latex}, oiblue, line width=1.5pt]
      (axis cs:1,1,0) -- (axis cs:1.5,1.5,0);
    \node[oiblue, anchor=north west, font=\small] at (axis cs:1.45,1.45,0)
      {$\mathbf u=\left\langle\tfrac{1}{\sqrt2},\tfrac{1}{\sqrt2}\right\rangle$};
    \node[anchor=south east, font=\small] at (axis cs:1,1,-0.12) {$(1,1)$};
  \end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</figure>
<!-- Hidden: superseded by fig-int-dirderiv-surface-slice-manim below, which shows
     the same construction. Uncomment to bring it back.
<figure xml:id="fig-int-dirderiv-surface-slice">
<caption>
Rotating the direction <m>\mathbf u</m> at <m>(1,1)</m> and reading off <m>D_{\mathbf u} f = \left(\nabla f\right)_{(1,1)}\cdot\mathbf u</m> as the slope of the tangent line to the sliced surface <m>z=1+x^2+y^2</m>.
</caption>
<interactive xml:id="interactive-dirderiv-surface-slice" iframe="dirderiv-surface-slice.html" width="100%" aspect="8:6">
<instructions>
<p>
Drag the slider to rotate the direction
<m>\mathbf u = \langle\cos\varphi, \sin\varphi\rangle</m> at the point
<m>(1,1)</m>. The vertical plane through <m>(1,1)</m> in the direction
<m>\mathbf u</m> cuts the surface <m>z = 1 + x^2 + y^2</m> in a curve,
and the tangent line to this curve at <m>(1,1,3)</m> has slope
<m>D_{\mathbf u} f = \left(\nabla f\right)_{(1,1)}\cdot\mathbf u</m>,
displayed above the figure. Use the buttons to snap <m>\mathbf u</m> to
the direction of <m>\nabla f</m>, its opposite, or a direction of zero
change. Drag the figure to view it from a different angle, or press the
<q>Rotate</q> button to spin it automatically.
</p>
</instructions>
</interactive>
</figure>

-->
<figure xml:id="fig-int-dirderiv-surface-slice-manim">
<caption>
The same slice, animated: the tangent to the sliced surface at
<m>(1,1,3)</m> tipping from steepest ascent through level to steepest
descent as the direction <m>\mathbf u</m> turns around <m>P_0(1,1)</m>.
</caption>
<interactive xml:id="interactive-dirderiv-surface-slice-manim" iframe="dirderiv-surface-slice-manim.html" width="100%" aspect="1:1">
<instructions>
<p>
Press <q>Play</q> to watch the vertical plane through <m>P_0(1,1)</m>
turn once around. The gold curve is where the plane cuts the surface
<m>z = 1 + x^2 + y^2</m>, and the straight line through <m>(1,1,3)</m>
is the tangent to that curve; its slope is
<m>D_{\mathbf u} f = \left(\nabla f\right)_{(1,1)}\cdot\mathbf u</m>,
read off at the lower left. The tangent is green and steepest upwards
when <m>\mathbf u</m> points along <m>\nabla f</m>, red and steepest
downwards along <m>-\nabla f</m>, and yellow and level in the two
directions tangent to the pink level curve. Stop the animation at any
time and drag the <m>\varphi</m> slider, drag the figure to change the
viewpoint, or press <q>Spin</q>.
</p>
</instructions>
</interactive>
</figure>
</solution>
</example>

</section>

<!-- ================================================================ -->
<section xml:id="sec-dirderiv-properties">
<title>Directions of Fastest Increase, Fastest Decrease, and No Change</title>

<p>
The directional derivative can be expressed as
<md>
<mrow>D_{\mathbf u} f \amp= \nabla f\cdot\mathbf u</mrow>
<mrow>\amp= \|\nabla f\|\,\|\mathbf u\|\cos\theta</mrow>
<mrow xml:id="eq-dirderiv-cos-theta" number="yes">\amp= \|\nabla f\|\cos\theta,</mrow>
</md>
where <m>\theta</m> is the angle between <m>\nabla f</m> and the unit
vector <m>\mathbf u</m>. The first line is
<xref ref="eq-dirderiv-gradient-relation"/>, obtained from the chain rule in
<xref ref="sec-gradient-vector"/>; the second is the geometric form of the dot
product; and the third uses <m>\|\mathbf u\| = 1</m>.
</p>

<remark xml:id="remark-dirderiv-three-consequences">
<title>Three Consequences of <m>D_{\mathbf u} f = \|\nabla f\|\cos\theta</m></title>
<p>
Only the factor <m>\cos\theta</m> depends on the direction, and it runs over
<m>[-1,1]</m> as <m>\mathbf u</m> turns through the circle of directions at
<m>P_0</m>. Its three extreme values single out three directions, drawn in
<xref ref="fig-dirderiv-angle-prefigure"/>:
</p>
<p>
<ul>
<li>
<p>
When <m>\nabla f</m> and <m>\mathbf u</m> are in the same direction
(<m>\cos\theta = 1</m>), <m>f</m> increases most rapidly and
<m>D_{\mathbf u} f = \|\nabla f\|</m>.
</p>
</li>
<li>
<p>
When <m>\nabla f</m> and <m>\mathbf u</m> have opposite directions
(<m>\cos\theta = -1</m>), <m>f</m> decreases most rapidly and
<m>D_{\mathbf u} f = -\|\nabla f\|</m>.
</p>
</li>
<li>
<p>
When <m>\nabla f</m> and <m>\mathbf u</m> are orthogonal
(<m>\cos\theta = 0</m>), <m>f</m> does not change, and
<m>D_{\mathbf u} f = 0</m>.
</p>
</li>
</ul>
</p>
</remark>

<figure xml:id="fig-dirderiv-angle-prefigure">
<caption>
Directions around <m>P_0</m>: <m>f</m> increases fastest along
<m>\nabla f</m>, decreases fastest along <m>-\nabla f</m>, and does not
change in the two directions orthogonal to <m>\nabla f</m>.
</caption>
<image width="52%">
<prefigure xmlns="https://prefigure.org" label="prefigure-dirderiv-angle">
<diagram dimensions="(320,320)" margins="10">
<coordinates bbox="(-1.9,-1.9,1.9,1.9)">
<circle at="an-circle" center="(0,0)" radius="1" stroke="#7F7F7F"
        dash="6 4" fill="none"/>
<vector at="an-grad" v="(1.405,0.655)" tail="(0,0)" stroke="#D55E00"
        thickness="3"/>
<vector at="an-u" v="(0.087,0.996)" tail="(0,0)" stroke="#0072B2"
        thickness="3"/>
<vector at="an-zero1" v="(-0.423,0.906)" tail="(0,0)" stroke="#7F7F7F"
        thickness="2"/>
<vector at="an-zero2" v="(0.423,-0.906)" tail="(0,0)" stroke="#7F7F7F"
        thickness="2"/>
<point at="an-P0" p="(0,0)" fill="black" alignment="sw">
<m>P_0</m>
</point>
<label at="an-grad-label" anchor="(1.5,0.8)" alignment="ne" color="#D55E00">
<m>\nabla f</m>
</label>
<label at="an-u-label" anchor="(0.09,1.1)" alignment="n" color="#0072B2">
<m>\mathbf u</m>
</label>
<label at="an-zero1-label" anchor="(-0.55,1.15)" alignment="nw">
<m>D_{\mathbf u}f=0</m>
</label>
<label at="an-zero2-label" anchor="(0.55,-1.15)" alignment="se">
<m>D_{\mathbf u}f=0</m>
</label>
<label at="an-formula" anchor="(0,-1.62)" alignment="c">
<m>D_{\mathbf u} f = \|\nabla f\|\cos\theta</m>
</label>
</coordinates>

<annotations>
<annotation ref="figure" text="A point P 0 surrounded by a dashed unit circle of possible directions. A long red arrow shows the gradient of f. A blue unit arrow shows a direction u making an angle theta with the gradient. Two gray arrows orthogonal to the gradient mark the directions of zero change. Below the circle is the formula: the directional derivative equals the magnitude of the gradient times cosine theta.">
  <annotation ref="an-grad" text="The gradient vector of f at P 0, drawn in red. Moving in this direction gives the most rapid increase, with directional derivative equal to the magnitude of the gradient."/>
  <annotation ref="an-u" text="A unit vector u, drawn in blue, making an angle theta with the gradient. Its directional derivative is the magnitude of the gradient times cosine theta."/>
  <annotation ref="an-zero1" text="A direction orthogonal to the gradient, where cosine theta is zero, so the directional derivative is zero and f does not change."/>
  <annotation ref="an-zero2" text="The opposite orthogonal direction, where the directional derivative is also zero."/>
  <annotation ref="an-circle" text="The dashed unit circle of all possible directions u at the point P 0."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>

<activity xml:id="activity-verify-cos-theta">
<title>Verifying the Three Special Directions</title>

<introduction>
<p>
The three cases listed above are all consequences of a single formula,
<m>D_{\mathbf u} f = \|\nabla f\|\cos\theta</m>. In this activity you will derive them
yourself and then check your answers against
<xref ref="fig-int-gradient-direction-angle"/>, where <m>\mathbf u</m> can be dragged
around the circle of directions.
</p>
</introduction>

<task xml:id="task-cos-theta-formula">
<statement>
<p>
Beginning with <m>D_{\mathbf u} f = \nabla f \cdot \mathbf u</m>, explain why the dot
product can be rewritten as <m>\|\nabla f\|\cos\theta</m>.
</p>
</statement>
<answer>
<p>
For any two vectors, <m>\mathbf a \cdot \mathbf b = \|\mathbf a\|\|\mathbf b\|\cos\theta</m>
with <m>\theta</m> the angle between them. Applying this to <m>\nabla f</m> and
<m>\mathbf u</m>,
<md>
<mrow>D_{\mathbf u} f = \nabla f \cdot \mathbf u</mrow>
<mrow>\amp= \|\nabla f\|\,\|\mathbf u\|\cos\theta</mrow>
<mrow>\amp= \|\nabla f\|\cos\theta ,</mrow>
</md>
where the last step uses <m>\|\mathbf u\| = 1</m>. Without that assumption the factor
<m>\|\mathbf u\|</m> would survive, and rescaling <m>\mathbf u</m> would change
<m>D_{\mathbf u} f</m> without changing the direction being measured.
</p>
</answer>
</task>

<task xml:id="task-cos-theta-extremes">
<statement>
<p>
Treat <m>\|\nabla f\|</m> as fixed and let <m>\theta</m> run over
<m>[0, 2\pi)</m>. What are the largest and smallest values of
<m>D_{\mathbf u} f</m>, and at which angles do they occur? For which angles is
<m>D_{\mathbf u} f = 0</m>?
</p>
</statement>
<answer>
<p>
Since <m>-1 \le \cos\theta \le 1</m>, the quantity <m>\|\nabla f\|\cos\theta</m> ranges
between <m>-\|\nabla f\|</m> and <m>\|\nabla f\|</m>. The largest value <m>\|\nabla f\|</m>
occurs at <m>\theta = 0</m>, with <m>\mathbf u</m> pointing along <m>\nabla f</m>; the
smallest, <m>-\|\nabla f\|</m>, occurs at <m>\theta = \pi</m>, with <m>\mathbf u</m>
pointing along <m>-\nabla f</m>. The directional derivative vanishes when
<m>\cos\theta = 0</m>, that is at <m>\theta = \pi/2</m> and <m>\theta = 3\pi/2</m>,
the two directions orthogonal to <m>\nabla f</m>.
</p>
</answer>
</task>

<task xml:id="task-cos-theta-concrete">
<statement>
<p>
Now take <m>f(x,y) = 1 + x^2 + y^2</m> at <m>P_0(1,1)</m>, the function of
<xref ref="example-dirderiv-definition"/>. Compute <m>\nabla f</m> and
<m>\|\nabla f\|</m> at <m>P_0</m>, write <m>D_{\mathbf u} f</m> as a function of
<m>\theta</m>, and give the three special unit vectors explicitly.
</p>
</statement>
<answer>
<p>
Here <m>\nabla f = 2x\,\mathbf i + 2y\,\mathbf j</m>, so
<m>(\nabla f)_{(1,1)} = 2\,\mathbf i + 2\,\mathbf j</m> and
<m>\|\nabla f\| = 2\sqrt 2</m>. Therefore
<md>
<mrow>
D_{\mathbf u} f = 2\sqrt 2 \,\cos\theta .
</mrow>
</md>
</p>
<p>
The fastest increase happens along
<m>\mathbf u = \frac{1}{\sqrt 2}\mathbf i + \frac{1}{\sqrt 2}\mathbf j</m>, giving
<m>D_{\mathbf u} f = 2\sqrt 2 \approx 2.83</m>, which is the value computed in
<xref ref="example-dirderiv-definition"/>. The fastest decrease happens along
<m>-\frac{1}{\sqrt 2}\mathbf i - \frac{1}{\sqrt 2}\mathbf j</m>, giving
<m>-2\sqrt 2</m>. There is no change along
<m>\pm\left(\frac{1}{\sqrt 2}\mathbf i - \frac{1}{\sqrt 2}\mathbf j\right)</m>, the two
directions orthogonal to <m>\nabla f</m>.
</p>
</answer>
</task>

<task xml:id="task-cos-theta-check">
<statement>
<p>
Check your work against the figure below. Drag
<m>\mathbf u</m> around the circle and confirm that the meter agrees with
<m>2\sqrt 2\cos\theta</m> at several angles. Then use the three buttons and compare the
readings with your answers to the previous task. As you drag, at what angle does the
reading change most quickly, and at what angle is it momentarily stationary?
</p>

<figure xml:id="fig-int-gradient-direction-angle">
<caption>
The directional derivative <m>D_{\mathbf u} f = \|\nabla f\|\cos\theta</m> as the unit vector <m>\mathbf u</m> rotates around <m>P_0</m>.
</caption>
<interactive xml:id="interactive-gradient-direction-angle" iframe="gradient-direction-angle.html" width="100%" aspect="7:6">
<instructions>
<p>
Drag the blue unit vector <m>\mathbf u</m> around the circle of
directions and watch the meter display
<m>D_{\mathbf u} f = \|\nabla f\|\cos\theta</m>. Use the buttons to snap
<m>\mathbf u</m> to the three special cases: the same direction as
<m>\nabla f</m> (fastest increase), the opposite direction (fastest
decrease), and a direction orthogonal to <m>\nabla f</m> (no change).
</p>
</instructions>
</interactive>
</figure>
</statement>
<answer>
<p>
The three buttons should give <m>2\sqrt 2 \approx 2.83</m>,
<m>-2\sqrt 2 \approx -2.83</m>, and <m>0</m>. The reading changes fastest where
<m>\left|\frac{d}{d\theta}\cos\theta\right| = |\sin\theta|</m> is largest, namely at
<m>\theta = \pi/2</m> and <m>3\pi/2</m>, the very directions where
<m>D_{\mathbf u} f</m> itself is zero. It is momentarily stationary at
<m>\theta = 0</m> and <m>\theta = \pi</m>, where <m>D_{\mathbf u} f</m> is largest and
smallest. So a small change of heading matters least when you are already pointing
straight uphill, and most when you are moving along a level direction.
</p>
</answer>
</task>

</activity>


<example xml:id="example-gradient-directions">
<title>Example III</title>
<statement>
<p>
Consider the function
<m>f(x,y) = \dfrac{x^2}{2} + \dfrac{y^2}{2}</m>. Find the directions in
which, at the point <m>P_0(1,1)</m>,
</p>

<p>
<ol marker="A.">
<li>
<p>
<m>f</m> increases most rapidly,
</p>
</li>
<li>
<p>
<m>f</m> decreases most rapidly,
</p>
</li>
<li>
<p>
there is no change in <m>f</m>.
</p>
</li>
</ol>
</p>
</statement>


<solution>
<p>
<em>A.</em> As explained above, <m>f</m> increases most rapidly in the
direction of <m>\nabla f</m>. We have
<md>
<mrow>\nabla f \amp= x\,\mathbf i + y\,\mathbf j \quad\Rightarrow\quad \left(\nabla f\right)_{(1,1)}</mrow>
<mrow xml:id="eq-directions-gradient" number="yes">\amp= \mathbf i + \mathbf j.</mrow>
</md>
</p>
<p>
Note that we need to divide
<m>\left(\nabla f\right)_{(1,1)} = \mathbf i + \mathbf j</m> by its
length to find the unit vector corresponding to the direction of most
rapid increase:
<md xml:id="eq-directions-increase" number="yes">
\frac{\left(\nabla f\right)_{(1,1)}}{\left\|\left(\nabla f\right)_{(1,1)}\right\|}
= \frac{1}{\sqrt 2}\mathbf i + \frac{1}{\sqrt 2}\mathbf j.
</md>
</p>

<p>
<em>B.</em> For the direction of most rapid decrease, we take the
opposite direction:
<md xml:id="eq-directions-decrease" number="yes">
-\frac{\left(\nabla f\right)_{(1,1)}}{\left\|\left(\nabla f\right)_{(1,1)}\right\|}
= -\frac{1}{\sqrt 2}\mathbf i - \frac{1}{\sqrt 2}\mathbf j.
</md>
</p>

<p>
<em>C.</em> To find the directions of zero change in <m>f</m>, we require
<md>
<mrow>\left(\nabla f\right)_{(1,1)}\cdot\mathbf u \amp= (\mathbf i + \mathbf j)\cdot(u_1\mathbf i + u_2\mathbf j)</mrow>
<mrow>\amp= u_1 + u_2</mrow>
<mrow>\amp= 0 \quad\text{or}\quad u_1</mrow>
<mrow xml:id="eq-directions-zero-dot" number="yes">\amp= -u_2,</mrow>
</md>
and since <m>\mathbf u</m> is a unit vector, we have
<md xml:id="eq-directions-unit-length" number="yes">
\|\mathbf u\|^2 = u_1^2 + u_2^2 = 1.
</md>
</p>

<p>
After substituting <xref ref="eq-directions-zero-dot"/> in
<xref ref="eq-directions-unit-length"/>, we conclude
<md xml:id="eq-directions-zero-solve" number="yes">
2u_1^2 = 1 \quad\Rightarrow\quad u_1 = \pm\frac{1}{\sqrt 2}.
</md>
</p>

<p>
Hence, the directions of zero change in <m>f</m> at <m>P_0(1,1)</m> are
<md>
<mrow>\mathbf u \amp= \frac{1}{\sqrt 2}\mathbf i - \frac{1}{\sqrt 2}\mathbf j \qquad\text{and}\qquad \tilde{\mathbf u}</mrow>
<mrow xml:id="eq-directions-zero-answer" number="yes">\amp= -\frac{1}{\sqrt 2}\mathbf i + \frac{1}{\sqrt 2}\mathbf j.</mrow>
</md>
</p>
<p>
Note that <m>\tilde{\mathbf u} = -\mathbf u</m>. All four directions are
shown in <xref ref="fig-gradient-directions-prefigure"/>: the directions of zero
change are tangent to the level curve of <m>f</m> through <m>P_0(1,1)</m>,
while the directions of most rapid increase and decrease are
perpendicular to it.
</p>
<figure xml:id="fig-gradient-directions-prefigure">
<caption>
At <m>P_0(1,1)</m>: fastest increase along
<m>\nabla f/\|\nabla f\|</m>, fastest decrease along
<m>-\nabla f/\|\nabla f\|</m>, and zero change along
<m>\pm\mathbf u</m>, tangent to the level curve <m>f = 1</m>.
</caption>
<image width="52%">
<prefigure xmlns="https://prefigure.org" label="prefigure-gradient-directions">
<diagram dimensions="(320,320)" margins="10">
<coordinates bbox="(-2.5,-2.5,2.7,2.7)">
<axes at="gd-axes" xlabel="x" ylabel="y" decorations="no" arrows="1"/>
<circle at="gd-level1" center="(0,0)" radius="0.707" stroke="#7F7F7F"
        fill="none"/>
<circle at="gd-level2" center="(0,0)" radius="1" stroke="#7F7F7F"
        fill="none"/>
<circle at="gd-level3" center="(0,0)" radius="1.414" stroke="#0072B2"
        thickness="2" fill="none"/>
<circle at="gd-level4" center="(0,0)" radius="1.732" stroke="#7F7F7F"
        fill="none"/>
<line at="gd-tangent" endpoints="((0.15,1.85),(1.85,0.15))" stroke="#7F7F7F"
      dash="6 4" thickness="1.5"/>
<vector at="gd-increase" v="(0.566,0.566)" tail="(1,1)" stroke="#009E73"
        thickness="3"/>
<vector at="gd-decrease" v="(-0.566,-0.566)" tail="(1,1)" stroke="#D55E00"
        thickness="3"/>
<vector at="gd-zero1" v="(0.566,-0.566)" tail="(1,1)" stroke="#0072B2"
        thickness="2"/>
<vector at="gd-zero2" v="(-0.566,0.566)" tail="(1,1)" stroke="#0072B2"
        thickness="2"/>
<point at="gd-P0" p="(1,1)" fill="black" alignment="e">
<m>P_0(1,1)</m>
</point>
<label at="gd-increase-label" anchor="(1.7,1.75)" alignment="ne"
       color="#009E73">
<m>\nabla f/\|\nabla f\|</m>
</label>
<label at="gd-decrease-label" anchor="(0.38,0.28)" alignment="sw"
       color="#D55E00">
<m>-\nabla f/\|\nabla f\|</m>
</label>
<label at="gd-zero1-label" anchor="(1.68,0.38)" alignment="se"
       color="#0072B2">
<m>\mathbf u</m>
</label>
<label at="gd-zero2-label" anchor="(0.38,1.68)" alignment="nw"
       color="#0072B2">
<m>\tilde{\mathbf u}</m>
</label>
</coordinates>

<annotations>
<annotation ref="figure" text="Concentric circles centered at the origin, the level curves of f of x comma y equals x squared over 2 plus y squared over 2. At the point P 0 with coordinates 1 comma 1, four unit arrows are drawn: a green arrow pointing outward along the gradient, the direction of fastest increase; a red arrow pointing inward opposite the gradient, the direction of fastest decrease; and two blue arrows tangent to the level circle through P 0, the directions of zero change. A dashed line tangent to the circle at P 0 contains the two blue arrows.">
  <annotation ref="gd-level3" text="The level curve f equals 1 passing through the point P 0, a circle of radius root 2, highlighted in blue."/>
  <annotation ref="gd-P0" text="The point P 0 with coordinates 1 comma 1, where the four special directions are drawn."/>
  <annotation ref="gd-increase" text="The green unit vector 1 over root 2 comma 1 over root 2, the direction of the gradient, in which f increases most rapidly."/>
  <annotation ref="gd-decrease" text="The red unit vector negative 1 over root 2 comma negative 1 over root 2, opposite to the gradient, in which f decreases most rapidly."/>
  <annotation ref="gd-zero1" text="The blue unit vector u equals 1 over root 2 comma negative 1 over root 2, a direction of zero change, tangent to the level curve."/>
  <annotation ref="gd-zero2" text="The blue unit vector u tilde equals negative u, the other direction of zero change, also tangent to the level curve."/>
  <annotation ref="gd-tangent" text="The dashed tangent line to the level curve at P 0, which contains both directions of zero change."/>
</annotation>
</annotations>
</diagram>
</prefigure>
</image>
</figure>
<!-- Hidden: superseded by fig-int-gradient-max-directions-manim below, which shows
     the same construction. Uncomment to bring it back.
<figure xml:id="fig-int-gradient-max-directions">
<caption>
The value of <m>D_{\mathbf u} f = u_1 + u_2</m> for <m>f(x,y)=\frac{x^2}{2}+\frac{y^2}{2}</m> as <m>\mathbf u</m> rotates around <m>P_0(1,1)</m>, with the special directions of Example III.
</caption>
<interactive xml:id="interactive-gradient-max-directions" iframe="gradient-max-directions.html" width="100%" aspect="7:6">
<instructions>
<p>
Drag the unit vector <m>\mathbf u</m> around the point <m>P_0(1,1)</m>
on the level-curve plot of
<m>f(x,y) = \frac{x^2}{2} + \frac{y^2}{2}</m> and watch the value of
<m>D_{\mathbf u} f = u_1 + u_2</m>. Use the buttons to snap
<m>\mathbf u</m> to the answers of Example III: the direction of
fastest increase, the direction of fastest decrease, and the two
directions of no change.
</p>
</instructions>
</interactive>
</figure>

-->
<figure xml:id="fig-int-gradient-max-directions-manim">
<caption>
The same three directions, animated: <m>D_{\mathbf u} f</m> sweeping
between <m>\|\nabla f\|</m> and <m>-\|\nabla f\|</m> as <m>\mathbf u</m>
turns around <m>P_0(1,1)</m>.
</caption>
<interactive xml:id="interactive-gradient-max-directions-manim" iframe="gradient-max-directions-manim.html" width="100%" aspect="1:1">
<instructions>
<p>
Press <q>Play</q> to watch <m>\mathbf u</m> turn once around
<m>P_0(1,1)</m>. The reading at the lower left follows
<m>D_{\mathbf u} f = \nabla f\cdot\mathbf u = \|\nabla f\|\cos\theta</m>
as it sweeps from <m>\|\nabla f\|</m> down to <m>-\|\nabla f\|</m> and
back: it is largest along <m>\nabla f</m> (A), smallest along
<m>-\nabla f</m> (B), and zero in the two directions tangent to the
highlighted level curve <m>f = 1</m> (C), where the second vector
<m>\tilde{\mathbf u} = -\mathbf u</m> appears. Stop the animation at any
time and drag <m>\mathbf u</m> on the picture, or use the slider.
</p>
</instructions>
</interactive>
</figure>

<!-- Hidden: superseded by fig-int-gradient-max-directions-3d-manim below, which shows
     the same construction. Uncomment to bring it back.
<figure xml:id="fig-int-gradient-max-directions-3d">
<caption>
A 3D view of Example III: the vertical slice of <m>z=\frac{x^2}{2}+\frac{y^2}{2}</m> in the direction <m>\mathbf u</m>, its tangent slope, and the level curve <m>f=1</m>.
</caption>
<interactive xml:id="interactive-gradient-max-directions-3d" iframe="gradient-max-directions-3d.html" width="100%" aspect="8:6">
<instructions>
<p>
A 3D view of Example III. The surface
<m>z = \frac{x^2}{2} + \frac{y^2}{2}</m> is shown with the level curve
<m>f = 1</m>, the circle <m>x^2 + y^2 = 2</m>, passing through the point
<m>(1,1,1)</m>; its dashed projection lies in the <m>xy</m>-plane. Drag
the slider to rotate the direction
<m>\mathbf u = \langle\cos\varphi, \sin\varphi\rangle</m> at
<m>P_0(1,1)</m>: the vertical plane in the direction <m>\mathbf u</m>
cuts the surface in a curve whose tangent line at <m>(1,1,1)</m> has
slope <m>D_{\mathbf u} f = \cos\varphi + \sin\varphi</m>. Use the
buttons to snap to the answers of Example III, and note that when
<m>D_{\mathbf u} f = 0</m>, the vector <m>\mathbf u</m> is tangent to
the level curve. Drag the figure to view it from a different angle, or
press the <q>Rotate</q> button.
</p>
</instructions>
</interactive>
</figure>

-->
<figure xml:id="fig-int-gradient-max-directions-3d-manim">
<caption>
The same 3D view, animated: the tangent to the sliced surface at
<m>(1,1,1)</m> tipping from steepest ascent through level to steepest
descent as <m>\mathbf u</m> turns.
</caption>
<interactive xml:id="interactive-gradient-max-directions-3d-manim" iframe="gradient-max-directions-3d-manim.html" width="100%" aspect="1:1">
<instructions>
<p>
Press <q>Play</q> to watch the vertical plane through <m>P_0(1,1)</m>
turn once around. The gold curve is where it cuts the surface
<m>z = \frac{x^2}{2} + \frac{y^2}{2}</m>, and the straight line through
<m>(1,1,1)</m> is the tangent to that curve, whose slope is
<m>D_{\mathbf u} f</m>: it is green and steepest upwards along
<m>\nabla f</m> (A), red and steepest downwards along <m>-\nabla f</m>
(B), and yellow and level in the two directions tangent to the pink
level curve <m>f = 1</m> (C). Stop the animation at any time and drag
the <m>\varphi</m> slider, drag the figure to change the viewpoint, or
press <q>Spin</q>.
</p>
</instructions>
</interactive>
</figure>
</solution>
</example>

</section>

<!-- ================================================================ -->

<!-- ================================================================ -->
<section xml:id="sec-gradient-summary">
<title>Summary</title>

<p>
We collect here the main ideas of this section.
</p>

<figure xml:id="video-dirderiv-2d-3d">
<caption>
A directional derivative viewed together in the plane and in space: the unit
vector <m>\mathbf u</m> at <m>P_0</m> in the <m>xy</m>-plane selects a slice of
the surface <m>z=f(x,y)</m>, and <m>\left(D_{\mathbf u} f\right)_{P_0}</m> is the
slope of the resulting tangent line to that slice.
</caption>
<video xml:id="video-dirderiv-definition-2d-3d"
       source="videos/dirderiv_definition_2D_3D.mp4"
       width="70%" aspect="16:9"/>
</figure>

<p>
<ul>
<li>
<p>
<em>The directional derivative.</em>
<xref ref="def-directional-derivative"/> defines
<m>\left(D_{\mathbf u} f\right)_{P_0}</m> as the limit in
<xref ref="eq-dirderiv-definition"/>; it measures the rate of change of
<m>f</m> at <m>P_0</m> in the direction of a unit vector <m>\mathbf u</m>.
It is computed directly from this limit in
<xref ref="example-dirderiv-definition"/>, and
<xref ref="video-dirderiv-2d-3d"/> shows how the plane and space pictures fit
together.
</p>
</li>
<li>
<p>
<em>The gradient vector.</em> Using the chain rule we obtain
<xref ref="def-gradient-vector"/>, which gives the shortcut
<m>\left(D_{\mathbf u} f\right)_{P_0}
= \left(\nabla f\right)_{P_0}\cdot\mathbf u</m> in
<xref ref="eq-dirderiv-gradient-relation"/>. This is applied in
<xref ref="example-dirderiv-gradient"/>.
</p>
</li>
<li>
<p>
<em>Fastest increase, fastest decrease, and no change.</em> Writing
<m>D_{\mathbf u} f = \|\nabla f\|\cos\theta</m> in
<xref ref="eq-dirderiv-cos-theta"/> shows that <m>f</m> increases most
rapidly in the direction of <m>\nabla f</m>, decreases most rapidly in
the direction of <m>-\nabla f</m>, and does not change in the directions
orthogonal to <m>\nabla f</m>. These directions are found in
<xref ref="example-gradient-directions"/>.
</p>
</li>
<li>
<p>
<em>The gradient is perpendicular to the level curves.</em>
<xref ref="thm-gradient-perp-level-curve"/> proves that <m>\nabla f</m> is
perpendicular to the level curve through each point. Equivalently, the
directions of no change are tangent to the level curve, while the
directions of most rapid increase and decrease are perpendicular to it.
</p>
</li>
</ul>
</p>

</section>

<xi:include href="subsec-scalar-fields-potentials.ptx"/>
<xi:include href="exercises-gradient-applications.ptx"/>

</chapter>
